<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SS14.Launcher.ViewModels;assembly=SS14.Launcher"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="SS14.Launcher.Views.OptionsTabView"
             Name="NewsTab">
    <Design.DataContext>
        <vm:OptionsTabViewModel />
    </Design.DataContext>

    <Grid Margin="4" VerticalAlignment="Top" ColumnDefinitions="Auto, *">
        <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Player Name:" Margin="4" />
        <TextBox Margin="4" VerticalAlignment="Center" Grid.Column="1" Text="{Binding PlayerName,Mode=TwoWay}" />
    </Grid>
</UserControl>