<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SS14.Launcher.ViewModels"
             mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="100"
             x:Class="SS14.Launcher.Views.ConnectingOverlay"
             Background="{DynamicResource WindowOverlayBrush}">

  <Design.DataContext>
    <vm:ConnectingViewModel />
  </Design.DataContext>
  <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
    <TextBlock HorizontalAlignment="Center" Classes="NanoHeadingMedium" Text="Connecting..." />
    <TextBlock HorizontalAlignment="Center" Margin="0 10" DockPanel.Dock="Top" Text="{Binding StatusText}" TextWrapping="Wrap" />
    <ProgressBar DockPanel.Dock="Bottom" Maximum="1" Value="{Binding Progress}"
                 IsIndeterminate="{Binding ProgressIndeterminate}" IsVisible="{Binding ProgressBarVisible}" MinWidth="300" />

    <TextBlock HorizontalAlignment="Right" Margin="0 4" DockPanel.Dock="Bottom" Text="{Binding ProgressText}" TextWrapping="Wrap"
               IsVisible="{Binding !ProgressIndeterminate}" />
  </StackPanel>
</UserControl>
