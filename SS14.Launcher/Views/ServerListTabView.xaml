<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SS14.Launcher.ViewModels;assembly=SS14.Launcher"
             xmlns:v="clr-namespace:SS14.Launcher.Views"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="SS14.Launcher.Views.ServerListTabView">
    <Design.DataContext>
        <vm:ServerListTabViewModel />
    </Design.DataContext>

    <DockPanel LastChildFill="True">
        <TextBlock DockPanel.Dock="Top" Text="Servers:" Classes="NanoHeading" />

        <!--
        <v:ClientUpdaterView DockPanel.Dock="Bottom" DataContext="{Binding Updater}" />
        -->

        <TextBox DockPanel.Dock="Bottom" Text="{Binding SearchString, Mode=TwoWay}" Watermark="Search For Servers..."
                 UseFloatingWatermark="False" />

        <Panel DockPanel.Dock="Bottom" Background="Red" Classes="ScrollViewerSep" />
        <Panel DockPanel.Dock="Top" Background="Red" Classes="ScrollViewerSep" />
        <ScrollViewer>
            <ItemsControl Items="{Binding SearchedServers}" />
        </ScrollViewer>
    </DockPanel>
</UserControl>