<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mainWindowTabs="clr-namespace:SS14.Launcher.ViewModels.MainWindowTabs"
             xmlns:mainWindowTabs1="clr-namespace:SS14.Launcher.Views.MainWindowTabs"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="SS14.Launcher.Views.MainWindowTabs.PatchesTabView"
             Name="NewsTab">
  <Design.DataContext>
    <mainWindowTabs1:PatchesTabView />
  </Design.DataContext>

  <ScrollViewer HorizontalScrollBarVisibility="Disabled">
  <Grid RowDefinitions="Auto, Auto, Auto">
    <DockPanel Grid.Row="0">
      <TextBlock DockPanel.Dock="Top" Margin="4, 0" Text="Plugins" Classes="NanoHeadingMedium" />
      <Button Content="Open patch directory" DockPanel.Dock="Top" Margin="4, 0" HorizontalAlignment="Left"
              Command="{Binding OpenPatchDirectory}"/>
      <Line></Line>
    </DockPanel>

    <DockPanel Grid.Row="1">
      <StackPanel Orientation="Vertical">
      <ItemsControl Items="{Binding Patches}" Name="PatchList">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <StackPanel Orientation="Horizontal">
              <CheckBox VerticalAlignment="Center" Margin="4" IsChecked="{Binding enabled}" Content="{Binding name}"></CheckBox>
              <TextBlock VerticalAlignment="Center" TextWrapping="Wrap"
                         Text="{Binding desc}"
                         Margin="8" />
            </StackPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
    </DockPanel>
  </Grid>

  </ScrollViewer>
</UserControl>
